## MODIFIED Requirements

### Requirement: Sync center management page
The desktop application SHALL provide a dedicated sync center page for manual sync execution and status visibility.

#### Scenario: Use sync center in packaged mode
- **WHEN** the packaged desktop app opens the sync center
- **THEN** the page can still render recent local logs and local sync state
- **AND** any packaged-mode sync limitations are documented rather than hidden
