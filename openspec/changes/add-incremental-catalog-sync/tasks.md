## 1. Spec And Contract

- [x] 1.1 Add the incremental catalog sync proposal, design, and spec artifacts
- [x] 1.2 Extend shared sync result types for incremental summary payloads

## 2. Crawler Incremental Export

- [x] 2.1 Add minimal incremental comparison for agents, weapons, and drive discs in the SQLite exporter
- [x] 2.2 Extend crawler task payloads and CLI output so Electron receives created / updated / unchanged statistics

## 3. Desktop Aggregation And UI

- [x] 3.1 Extend Python task parsing, sync catalog aggregation, and sync log parsing with incremental summaries
- [x] 3.2 Update the sync center page to show incremental sync summaries for the latest run
- [x] 3.3 Update MVP status and verification notes for incremental sync
